import React, { FC } from "react";
import { Typography } from "@mui/material";

const ChatHeader: FC = () => {
  return (
    <Typography variant="h4" align="left" gutterBottom>
      Speech Shadowing Chatbot
    </Typography>
  );
};

export default ChatHeader;
